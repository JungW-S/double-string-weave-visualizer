# Double String Weave Visualizer

Static HTML visualizer for the double inductive weave attached to an input double string.

Open `index.html` through a static server or GitHub Pages.

The page accepts entries such as `1L`, `2R`, and `1L+`; plus signs are recomputed automatically by the Demazure-product length test.
